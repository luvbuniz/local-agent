# Optional: deploy from the command line with `npx wrangler deploy`
# (the dashboard copy-paste route in grok-voice-relay.js works too).
# Set the secret once with: npx wrangler secret put XAI_API_KEY
name = "grok-voice-relay"
main = "grok-voice-relay.js"
compatibility_date = "2026-07-01"
